# This file is unused — libjpeg-turbo is built directly via its own CMake.
# See build.sh for the actual build process.
#
# libjpeg-turbo does not support add_subdirectory(); it must be built
# as a standalone project with its own CMake invocation.
